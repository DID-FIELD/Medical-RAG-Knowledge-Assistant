import os

# ==================================================
# 路径配置（自动适配跨平台，无需手动修改）
# ==================================================
# 项目根目录：当前文件在 src/ 下，向上两级即为根目录
ROOT_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# 原始医疗文档存放目录
DATA_DIR = os.path.join(ROOT_DIR, "data", "medical_docs")

# Chroma向量库持久化目录
VECTOR_DB_PATH = os.path.join(ROOT_DIR, "vectorstore", "chroma_db")

# ==================================================
# 文档分块参数
# ==================================================
# 单个知识块最大字符数
CHUNK_SIZE = 500
# 相邻块重叠字符数，避免知识截断
CHUNK_OVERLAP = 50

# ==================================================
# 嵌入模型配置
# ==================================================
# 嵌入模型配置（Ollama本地版）
EMBEDDING_MODEL = "nomic-embed-text"

# ==================================================
# 大模型配置（Ollama本地部署）
# ==================================================
# 模型名称：速度优先用3b，追求效果换7b
LLM_MODEL = "qwen2.5:3b"
# Ollama本地服务默认地址
OLLAMA_BASE_URL = "http://localhost:11434"
# 生成温度：越低越稳定，幻觉越少
LLM_TEMPERATURE = 0.1

# ==================================================
# 检索配置
# ==================================================
# 单次问答召回的相关文档数量
RETRIEVE_TOP_K = 3